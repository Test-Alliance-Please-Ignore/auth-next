/**
 * Migration imports for {{ pascal name }}
 * These are imported at build time and embedded in the Worker
 */

// Import all migration SQL files as raw strings
// @ts-expect-error - Build tools handle ?raw imports
import migration001 from '../migrations/{{ pascal name }}/001_initial_schema.sql?raw'

// Export as a mapping for the loader
export const {{ camelCase name }}Migrations: Record<string, string> = {
	'001_initial_schema.sql': migration001,
}