/**
 * {{ pascal name }} Durable Object Interface
 *
 * {{ description }}
 *
 * The actual implementation lives in apps/{{ workerName }}/src/{{ slug name }}.ts
 */

// ========== Types ==========

// Add your custom types here

// ========== Durable Object Interface ==========

/**
 * {{ pascal name }} Durable Object Interface
 *
 * {{ description }}
 *
 * Note: This interface is used for type-safe cross-worker communication.
 * The actual implementation is in apps/{{ workerName }}/src/{{ slug name }}.ts
 */
export interface {{ pascal name }} {
	/**
	 * Example method - replace with your actual methods
	 */
	exampleMethod(): Promise<string>
}
