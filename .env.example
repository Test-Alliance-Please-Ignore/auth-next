# Cloudflare Credentials
# Required for deploying Workers locally with `just deploy`
#
# Get your account ID from: https://dash.cloudflare.com/
# Create an API token at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: "Edit Cloudflare Workers"

CLOUDFLARE_ACCOUNT_ID=your_account_id_here
CLOUDFLARE_API_TOKEN=your_api_token_here

# Neon Database
# Required for database access from Workers
#
# Get your connection string from: https://console.neon.tech/
# Use the pooled connection string for production use
# The non-pooled URL is used for migrations and admin tasks

DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
DATABASE_URL_MIGRATIONS=postgresql://user:pass@host/dbname?sslmode=require
