/**
 * Database schema for the orchestrator worker
 *
 * This file re-exports the schema from the @repo/orchestrator package.
 * The package is the single source of truth for schema definitions.
 */

export { schema, workflowInstances } from '@repo/orchestrator'

