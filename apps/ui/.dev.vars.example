# Discord OAuth Configuration
# Get your Discord application credentials from https://discord.com/developers/applications
VITE_DISCORD_CLIENT_ID=your_discord_client_id_here

# Optional: force frontend API calls through the local UI proxy.
# Recommended for `just dev-local` / `bun runx dev-local`.
VITE_API_BASE_URL=/api
